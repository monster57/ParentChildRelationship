var parentChildData = require("../../Data/ParentChildData.json");
var parentFact = require("../../Data/ParentFact.json")

// console.log(parentChildData);
console.log(parentFact);
console.log(parentFact.length)